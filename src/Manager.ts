import { Parser } from './Parser';

export class Manager{
  public static parser: Parser;

  public static parse(problem: string): string {
    // to do
    return null;
  }

  public static chooseParse(prefix: string): void {
    // to do
    Manager.parser = null;
  }

}
